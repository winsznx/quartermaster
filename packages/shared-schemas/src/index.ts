// Shared zod schemas — the contract between daemon, dashboard, and landing.
// Phase 2 fills this in. Until then, this file exports the schema names that
// downstream code can reference, so a wire-up exists today and only the bodies
// change.
//
// Per PRD §22.3 the daemon HTTP API shape (returned by these endpoints) is
// what these schemas describe.

export {};
